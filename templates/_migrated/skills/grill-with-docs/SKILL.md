---
name: grill-with-docs
description: A relentless interview to sharpen a plan or design, which also creates docs (ADR's and glossary) as we go.
disable-model-invocation: true
---

<!-- Vendored from https://github.com/mattpocock/skills (skills/engineering/grill-with-docs) -->

Run a `/grilling` session, using the `/domain-modeling` skill.

House rules: questions, context and recommendations to the human are in
Russian. If the project already has an ADR registry and glossary
(`docs/ADR/` + index, `GLOSSARY.md`), record into those in their format
instead of creating the `CONTEXT.md` / `docs/adr/` convention.
