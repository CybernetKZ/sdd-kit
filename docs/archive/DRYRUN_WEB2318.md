# Ручной прогон sdd-kit на WEB-2318 - журнал фрикции

> Цель прогона: **не выполнить тикет**, а проверить кит в боевом режиме и починить всё,
> что мешает. Полигон: `/home/octrow/dev/web-backend-new` (WBN @ `efbfec54`), store
> `cybernet-specs`. Начало 2026-08-03.

## Найдено до прогона (окружение)

| # | Проблема | Статус |
|---|---|---|
| E1 | `sdd-doctor` не знал про агента `test-author` - репортил его как "extra agent" | **исправлено** (kit `f16a732`-1) |
| E2 | Цель `test` в Makefile.sdd конфликтует с родным `test` целевого репо (`warning: overriding recipe`) | **исправлено**: переименована в `sdd-test` (kit `f16a732`) |
| E3 | `make sdd-index` падал с `error: no LLM API key found` - на подписке Claude ключа нет | **исправлено**: без ключа не пытается extract, подсказывает интерактивный `/graphify`; апдейты индекса ключа не требуют |
| E4 | `make sdd-index` не упоминался в финальных шагах установки | **исправлено**: п. 4b в wrap-up `install.sh` |

## Шаг 1 (intake) - фрикция C1-C10

| # | Проблема | Приоритет | Статус |
|---|---|---|---|
| C1 | YouTrack MCP объявлен в `.mcp.json`, но инструментов `mcp__youtrack__*` в сессии нет; в скилле нет fallback | высокий | **отложено** (решение владельца 2026-08-03: YouTrack-вопросы пропускаем) |
| C2 | Шаг 1 отправляет в `openspec/specs/`, а в brownfield-репо он пуст; нет указания "опирайся на store + код / позови spec-miner" | высокий | **исправлено**: brownfield-оговорка + spec-miner |
| C3 | `openspec view` показывает дашборд и не читает store; правильная последовательность (`store list` -> `list --specs --store` -> `show --store`) в скилле не описана | высокий | **исправлено**: рабочая последовательность store-команд (проверена) |
| C4 | Store резолвится в локальную копию; нет проверки свежести и команды обновления (спека датирована `36d29f5e`, репо на `efbfec54`) | средний | **исправлено**: проверка свежести клона + `Last verified` как зацепка |
| C5 | Нет подсказки "построить индекс, если его нет" (и когда это оправдано) | средний | **исправлено**: подсказка про первичную сборку/`/graphify` |
| C6 | Не сказано, **куда** писать результат интейка до создания change - теряется при переходе к шагу 2 | высокий | **исправлено**: `openspec/changes/<id>/intake.md`, planner сворачивает в proposal |
| C7 | "Post as a ticket comment" невыполнимо без MCP; нет запасного пути (выдать текст для ручной вставки) | средний | **отложено** (там же) |
| C8 | Скилл ссылается на 11 ADR, которых в целевом репо нет (живут в ките) | средний | **исправлено**: одна строка "ADR живут в ките, скилл самодостаточен" |
| C9 | Нет правила разрешения "tier=deep + объём > 2 дней" -> сразу эпик | низкий | **исправлено**: "tier и размер независимы - deep+>2д = эпик сразу" |
| C10 | Адресат вопросов захардкожен ("Dina/Olga"), а RAISE-владелец может быть не назначен | низкий | **исправлено**: ролевой адресат вместо имён |

## Graphify на живом графе (WBN, 39 570 узлов) - проверено 2026-08-03

| # | Находка | Статус |
|---|---|---|
| G1 | `make sdd-index` инкрементально обновляет существующий граф **без API-ключа** (AST-only) - ветка фикса E3 подтверждена на живом репо | ок |
| G2 | **исправлено.** Формулировка в скилле вредила: `graphify query "<ticket claim>"` со свободным вопросом даёт шум - BFS стартует от заглавных слов ("Validated", "API", "keys") и вытаскивает фикстуры/старые planning-доки вместо кода | **исправлено**: probe by symbol, явное предупреждение про шум |
| G3 | Работают точечные запросы: `graphify explain "<symbol>"` (даёт файл:строку, тип, связи) и `graphify query "<symbol1> <symbol2>"` по именам сущностей; для влияния - `graphify affected "<X>"`, для связи - `graphify path "A" "B"` | **исправлено**: explain->affected->path->query, EXTRACTED vs INFERRED |
| G4 | Граф >5000 узлов не рендерит `graph.html` (лимит viz) - не ошибка, но стоит знать | инфо |

## Шаг 2 (planner) - фрикция P1-P6

Прогон: change `web-2318-web-call-session` создан, `openspec validate --strict` - valid
(12 Requirements / 37 Scenarios). Артефакт побочный; ценность - список ниже.

| # | Проблема | Приоритет | Статус |
|---|---|---|---|
| P1 | **Субагент не может вызвать слэш-команду.** Скилл и `planner.md` предписывают `/opsx:propose`, но у субагента нет такого инструмента. Обход (сработал): читать `.claude/commands/opsx/propose.md` и идти по шагам через CLI - `openspec new change` -> `openspec status --json` -> `openspec instructions <artifact> --json` | высокий | **исправлено**: `planner.md`, секция "How to create the change" - слэш-команда, если доступна, иначе проверенная CLI-последовательность |
| P2 | **`planner.md` противоречит SKILL.md по эпикам**: planner п.4 требует "tasks.md = порядок для ЕДИНСТВЕННОГО PR, не выдумывай multi-PR", а SKILL §2 + ADR-0013 требуют разбивать эпик на несколько PR под одной change | высокий | **исправлено**: `planner.md` правило 5 - один PR по умолчанию, эпик (решён на интейке) группирует задачи по PR |
| P3 | **Формат метаданных спек расходится**: `spec-miner` производит `id:`/`enforced:`, а ни одна спека в сторе их не содержит (чистая проза + якоря `file.py:line`). Конвенция кита не зафиксирована нигде | средний | **исправлено**: ADR-0017 + `planner.md` правило 4 + `test-author.md` (трейсер без `id`) |
| P4 | `openspec list --type spec --store <id>` из `propose.md` не существует в v1.7.0 (`error: unknown option '--type'`); список спек стора приходит в `openspec instructions ... --json` (поле `references`) | средний | **исправлено**: в файлах кита этой команды не было (агент собрал её сам); правильные команды закреплены в `planner.md`. `--type` есть у `show`, у `list` - `--specs` |
| P5 | **Нет конвенции "change в репо меняет стор-контракт"**: локальный `openspec/specs/` пуст, все контракты в сторе, но как оформить их правку из репо-change - в ките не описано (агент вынес это отдельными задачами на стор-PR) | высокий | **исправлено**: ADR-0018 + `planner.md` правило 6 + абзац в README/README_RU |
| P6 | **У deep-тира нет места для архитектурного сравнения**: SKILL требует "записать решение как ADR", но `docs/ADR/` в целевом репо нет (ADR живут в ките), а артефакт `design.md` внутри change скилл не упоминает | средний | **исправлено** в `planner.md` (правило 7); строку в SKILL §1b/§2 применяет владелец скиллов |

## M0. Чистый полигон (install.sh с нуля) - проверено 2026-08-03

Установка прошла: doctor 0 fail, store подцепился, `make sdd-index` дал задуманную
подсказку (E3 подтверждён на чистом клоне). Найдено:

| # | Проблема | Приоритет | Статус |
|---|---|---|---|
| M0-1 | Wrap-up install.sh противоречил ADR-0015: шаги 2-3 подавали branch protection + required check как обязательные ручные шаги установки | средний | **исправлено**: один пункт "enforcement advisory по умолчанию (ADR-0015), включение - отдельное решение владельца" |
| M0-2 | Двойное сообщение про AGENTS.md: `created: ... (profile payload)` и следом `exists: ... (left alone)` (payload-цикл + generic put) | низкий | **исправлено**: put для AGENTS.md пропускается, если файл уже есть |
| M0-3 | Ожидания плана M0 неточны: `git status` не пуст (`.env.capture`, `extra_scripts/` не в .gitignore WBN) и ruff-замечание неверно (конфиг в `backend/pyproject.toml`, в git - инсталлер корректно оставил repo's own) | низкий | **исправлено**: план уточнён, дефекта кита нет |
| M0-4 | `/graphify` - Unknown command в свежей сессии: скилл лежит в `~/.agents/skills/graphify/`, симлинка в `~/.claude/skills/` нет, а install.sh проверял только CLI (`command -v graphify`) и рапортовал "ok". Все подсказки кита ("run /graphify") были невыполнимы буквально (класс P1/G2) | высокий | **исправлено**: симлинк создан + install.sh чинит/создаёт его сам (`link_graphify_skill`); подтверждено пере-прогоном M0 - `/graphify` собрал граф (AST-only, 24k узлов) |
| M0-5 | `graphify-out/` (graph.json ~30 МБ) не заигнорирован - торчит в `git status` и может уехать в коммит; кит ставит `make sdd-index`, но не исключает его артефакт | средний | **исправлено**: install.sh (шаг 8c) и `--refresh` пишут `graphify-out/` в `.git/info/exclude` |

## M1. Хуки в живой сессии - проверяется 2026-08-03

M1.1+M1.2 пройдены одним заходом: spec-guard заблокировал Edit guard-файла (exit 2),
агент создал change `cleanup-agent-wb-model-dead-comment` (через Skill
openspec-propose - путь P1 работает), повторная правка прошла, change валиден.

| # | Проблема | Приоритет | Статус |
|---|---|---|---|
| M1-1 | Сообщение spec-guard двусмысленно: "Create a change (/opsx:propose) **or** mark it skip_specs: true" читается как альтернатива change'у, хотя `skip_specs` живёт в метаданных уже созданной change и машинно никем не читается (только конвенция AGENTS.md). Агент истолковал верно, но случайно | низкий | **исправлено**: формулировка "Create a change first...; refactor/tooling work needs one too - add skip_specs: true to its .openspec.yaml" (проверена на живом хуке) |
| M1-2 | Любая активная change открывает **все** guard-пути (хук не сверяет файл с change) - осознанная простота из докстринга хука ("no AST/glob until it actually hurts"), advisory-first | инфо | принято, не чиним |

M1.3 пройден без находок: `block-no-verify.cjs` (репо-версия кита) заблокировал
`git commit --no-verify`, агент сам откатился на обычный коммит, pre-commit с
sdd-check прошёл (`bd37baf2`, ветка `test-sdd-kit`, в коммите ровно 4 файла задачи).

M1.4 (`/compact`): `last-session-state.md` записан, ветка настоящая (`test-sdd-kit` -
хардкод `dev` починен), diff непустой. Одна находка:

| # | Проблема | Приоритет | Статус |
|---|---|---|---|
| M1-3 | База диффа в pre-compact - `origin/HEAD` (= `origin/main`), а рабочая база воркфлоу - `dev`: в survival-пакет попал чужой коммит с dev (`kz-prod-port.yaml`, WEB-2320) - после компакта читается как незавершённая работа сессии | низкий | **исправлено**: база `origin/dev`, если существует, иначе origin/HEAD, иначе `dev` (проверено: diff = ровно 4 файла сессии) |

M1.5 пройден без находок: правки `README.md` (вне 35 guard-путей) прошли без единого
hook-сообщения. **Блок M1 закрыт**: 3 находки (M1-1 сообщение spec-guard, M1-2 инфо,
M1-3 база диффа pre-compact), все закрыты.

## M2. Pre-commit на живых коммитах - проверяется 2026-08-03

M2.1 чист: коммит только README.md - "make sdd-check skipped (no spec-related
changes staged)", как обещано.

| # | Проблема | Приоритет | Статус |
|---|---|---|---|
| M2-1 | **AGENTS.md заигнорирован трекаемым .gitignore WBN** (строки 147-148: `CLAUDE.md`, `AGENTS.md`) - `git add` отказывает, главный артефакт кита никогда не доедет до git/CI/коллег. `sdd-doctor` при этом рапортовал "AGENTS.md present" без предупреждения | высокий | **исправлено** (доктор): WARN "present but gitignored" + подсказка убрать из .gitignore. Само снятие игнора - решение владельца в репо WBN (донор тоже затронут) |
| M2-2 | spec-lint дампит JSON в stdout безусловно: сейчас одна голая строка в выводе pre-commit, а после засева спек - многострочный `indent=2` пейлоад на каждом коммите; машинного потребителя у stdout нет | низкий | **исправлено**: JSON только по флагу `--json`, по умолчанию человекочитаемый отчёт (stderr), оба режима проверены |

M2.2 иначе чист: sdd-check реально отработал на коммите `openspec/config.yaml`
(`89d54bd3`): validate --strict, лимит AGENTS.md, sdd-flags; spec-lint назван advisory.
Попутно подтверждён ADR-0017: 224 metadata violations при ручном прогоне spec-lint по
стору - задокументированный шум, в сторе spec-lint не запускается.

M2.3 чист: `AKIA[0-9A-Z]{16}` в staged-файле заблокировал коммит, HEAD не сдвинулся.

| # | Проблема | Приоритет | Статус |
|---|---|---|---|
| M2-3 | **Ruff-автофикс может породить пустой коммит**: если автофикс полностью откатывает staged-правку (M2.4: `ea80bdbd` "add incorrect format" - пустой), git создаёт коммит, потому что его проверка на "пусто" идёт до pre-commit-хука | средний | **исправлено**: после ре-стейджа `git diff --cached --quiet` -> блок "ruff autofix reverted all staged changes" (проверено в песочнице: мусорный коммит больше не создаётся) |

M2.5 чист: коммит в `dev` - WARN + пропущен ("prefer a branch + pull request",
`2b2bbcde` создан); блок в protected-ветку показан в песочнице на `master`
("commit blocked", override `SDD_ALLOW_PROTECTED=1`). **Блок M2 закрыт**: 3 находки
(M2-1 AGENTS.md в .gitignore, M2-2 JSON-шум spec-lint, M2-3 пустой коммит после
автофикса), все закрыты.

## M3. Скилл целиком по триггеру ("сделай WEB-2318") - прогон 2026-08-03

Лог: `~/dev/web-backend-new/wbn-create-task-web-2318-sdd-kit.txt` (Opus 5).
Скилл прошёл весь цикл сам: intake -> proposal/design/spec-delta/tasks ->
test-author (RED) -> реализация -> mutation-тесты -> backend-reviewer -> честный
отчёт "что не сделано" (E2E, PR, YouTrack).

Что подтвердилось (фиксы предыдущих волн работают):
- триггер по "сделай WEB-2318" - скилл подхвачен сам (M3.1);
- YouTrack 403 -> graceful fallback на вставленный текст тикета;
- store опрошен, контракт `external-webapi-authorization` реально применён
  (Bearer+401 из тикета отклонены по контракту, вопрос задан владельцу) - C3/C4;
- все 3 ложных утверждения тикета найдены заново (WebRTC нет, ключи валидирует
  gateway+SSO, путь конфликтует) - цель intake достигнута;
- intake.md создан и затем свёрнут planner'ом в proposal.md с удалением -
  ровно по SKILL §1 (C6); proposal содержит Why/Deviations/Tier/Grill/Open questions;
- deep-тир завёл design.md (P6), change валиден --strict (M3.3);
- test-author: RED до реализации, 3 green-stub-диспута (loguru caplog, дубль
  теста, getdel-мутация), спор автор<->реализатор без правки чужих тестов -
  ADR-0016 работает (M3.4).

| # | Проблема | Приоритет | Статус |
|---|---|---|---|
| M3-1 | **Docker compose project-name collision**: два чекаута WBN делят имя проекта, `docker compose exec` цепляется к чужому дереву - test-author гонял тесты по чужому чекауту и оставил stray-файлы в доноре; стоило цикла отладки | средний | **исправлено** (test-author.md): секция "Running inside Docker" - проверить bind mount, предпочитать `docker compose run --rm --no-deps` |
| M3-2 | Ожидание плана M3.2 ("создан intake.md") противоречило SKILL §1 (planner сворачивает и удаляет) - проверяющий примет штатное поведение за пропажу артефакта | низкий | **исправлено**: план уточнён |
| M3-3 | Пожелания владельца по workflow (сложность->модель, ревью плана отдельным шагом, исполнение sonnet'ом/параллельно) | - | занесены в DEFECTS_BACKLOG (kit-flow 1-3), не для этого прогона |

## M4. Слэш-команды и graphify - проверено 2026-08-03

M4.1 чист: `/opsx:propose` в интерактивной сессии увидел существующую change,
не клобберил, предложил варианты; слэш- и CLI-путь (planner.md) дают одинаковую
структуру. M4.2 закрыт при пере-прогоне M0 (первичная сборка без ключа).
M4.3 чист: `make sdd-index` при существующем графе - инкрементально, AST-only,
без ключа (G1/E3 подтверждены).

| # | Проблема | Приоритет | Статус |
|---|---|---|---|
| M4-1 | **`graphify affected` не существует** - ни в CLI (path/explain/diagnose/query/...), ни в скилле graphify. G3-фикс вписал выдуманную команду в 3 файла кита (feature-flow, incident-flow, AGENTS.md-шаблон), нарушив правило "каждую команду сначала выполни" | высокий | **исправлено**: заменено на `query "<sym>"` (BFS fan-out ≈ blast radius, проверено: 85 узлов от AgentWB) + честная оговорка "точные reverse deps - grep/ast-grep, граф держит узел на файл" |
| M4-2 | `graphify explain` работает (файл:строка, typed edges), но Depends()-инъекции FastAPI не попадают в рёбра AST - degree узла это покрытие экстракции, не реальное использование; симвл дублируется по файлам (13 узлов AgentWB) | инфо | ограничение graphify, оговорка про INFERRED/verify-in-code в ките уже есть |

## M5. Make-цели и store - проверяется 2026-08-03

M5.1 чист: doctor 0 fail; WARN про gitignored AGENTS.md - это работающий фикс M2-1.
M5.3 чист: validate 2 passed, spec-lint advisory, JSON-шума нет (фикс M2-2 виден).

| # | Проблема | Приоритет | Статус |
|---|---|---|---|
| M5-1 | doctor флажил `repo-auditor` (штатный агент манифеста) как "extra agent" - список известных агентов не обновлён при добавлении агента (рецидив E1) | низкий | **исправлено**: repo-auditor в списке известных (проверено на полигоне: info исчез) |
| M5-2 | **`make sdd-test` врёт в монорепо**: "no ruff config" при живом `backend/pyproject.toml [tool.ruff]` и "no tests found" при 827 тестах в `backend/tests` - детекция смотрит только в корень | средний | **исправлено**: `SDD_TEST_CMD` override (задаётся в корневом Makefile до `-include Makefile.sdd` или в env; проверены все 3 ветки: skip/delegate/fail) + честные сообщения "at the repo root ... set SDD_TEST_CMD" |

M5.2: `warning: overriding recipe` отсутствует (фикс E2 подтверждён).
M5.4 чист: `make sdd-review` на подписке - "LGTM - no CRITICAL/HIGH issues".
M5.5 чист: store list (1 стор) -> list --specs (все 8 спек) -> show
external-webapi-authorization (363 строки) из свежего клона. **Блок M5 закрыт**:
2 находки (M5-1 repo-auditor "extra", M5-2 sdd-test в монорепо), обе закрыты.

## Итог прогона M0-M5 (2026-08-03)

Все 5 блоков пройдены. Новых находок за прогон: 13 (M0: 5, M1: 3, M2: 3, M3: 3,
M4: 2, M5: 2, минус инфо-строки), из них высоких - 3 (M0-4 /graphify unknown,
M2-1 AGENTS.md в .gitignore, M4-1 выдуманный `graphify affected`). Все закрыты в
ките и раскатаны по 6 боевым репо + полигон. Открытым остаётся только решение
владельца по .gitignore WBN (M2-1) и бэклог kit-flow(1-3).
