# Ручной прогон sdd-kit: план (вы выполняете — я мониторю)

> Цель: найти дефекты кита, которые невозможно поймать субагентом. Журнал находок —
> [DRYRUN_WEB2318.md](DRYRUN_WEB2318.md). Полигон: `/home/octrow/dev/web-backend-new`
> (тестовая копия; в `~/cybernet/web-backend-new` есть живой graphify-индекс — для M2.3).

## Почему именно вы

Субагент физически не может: вызвать слэш-команду (`/opsx:propose`, `/graphify`),
запустить скилл по триггеру, **инициировать срабатывание хуков** (PreToolUse/PreCompact
живут в вашей интерактивной сессии, у субагента их нет), поработать с YouTrack MCP.
Именно там и прячется оставшаяся фрикция: P1 (слэш-команды) уже подтвердил это.

## Мой мониторинг

После каждого блока я сам смотрю: `git status`/`diff` в ките и целевом репо, содержимое
созданных артефактов, `openspec validate`, вывод `sdd-doctor`. **От вас нужно минимум** —
скажите «M1.2 done» (или вставьте вывод, если он неожиданный). Дальше: заношу в журнал,
чиню, коммичу, раскатываю `--refresh` и говорю, что можно продолжать.

---

## M1. Хуки (главный блок — только реальная сессия)

Открыть Claude Code в `/home/octrow/dev/web-backend-new`.

| # | Что сделать | Ожидаемое |
|---|---|---|
| M1.1 | Попросить Claude отредактировать файл под guard-путём (напр. `backend/app/agent_wb/model/agent_wb.py`) **без активной change** | `spec-guard.cjs` блокирует (exit 2) с внятным сообщением про `/opsx:propose` |
| M1.2 | То же, но при активной change (`openspec/changes/web-2318-web-call-session/` уже есть) | правка проходит |
| M1.3 | Попросить сделать коммит с `--no-verify` | `block-no-verify.cjs` блокирует |
| M1.4 | Довести сессию до авто-компакта (или дождаться) | появился `.claude/last-session-state.md`, в нём ветка/коммит/активные changes; **проверить, что diff-секция не пустая** (чинили хардкод `dev`) |
| M1.5 | Правка файла **вне** guard-путей (напр. `README.md`) | хуки молчат |

## M2. Слэш-команды и graphify

| # | Что сделать | Ожидаемое |
|---|---|---|
| M2.1 | `/opsx:propose "WEB-2318: web-call session"` в чистой ветке | change создаётся; сравню с тем, что сделал субагент через CLI (P1) |
| M2.2 | `/graphify` в `~/dev/web-backend-new` (индекса нет) | первичная сборка идёт через сессию, без API-ключа — это ровно тот путь, что я вписал в `make sdd-index` |
| M2.3 | В `~/cybernet/web-backend-new`: `graphify explain "verify_api_key"`, затем `graphify affected "AgentWb"` | символьные пробы дают код, а не шум (проверка новой формулировки в скилле) |

## M3. Скилл целиком

| # | Что сделать | Ожидаемое |
|---|---|---|
| M3.1 | В новой сессии сказать: «сделай WEB-2318» (текст тикета под рукой) | скилл `feature-flow` подхватывается **сам** по описанию-триггеру |
| M3.2 | Пройти шаг 1 как написано | агент создаёт `openspec/changes/<id>/intake.md` (новая конвенция C6), лезет в store правильными командами (C3) |
| M3.3 | Дойти до шага 2 | planner подхватывает `intake.md` и сворачивает его в proposal |

## M4. Pre-commit на живом коммите

| # | Что сделать | Ожидаемое |
|---|---|---|
| M4.1 | Закоммитить правку `.md`-файла | `make sdd-check` **пропущен** («no spec-related changes staged») — фикс 6.6 |
| M4.2 | Закоммитить правку в `openspec/` или `AGENTS.md` | `sdd-check` реально отрабатывает |
| M4.3 | Попробовать закоммитить строку вида `AKIA...`/`sk-ant-...` в тестовом файле | блок по секретам |
| M4.4 | Закоммитить `.py` с кривым форматированием | ruff автофиксит и ре-стейджит |

## M5. Make-цели в живом репо

| # | Команда | Ожидаемое |
|---|---|---|
| M5.1 | `make sdd-doctor` | 0 fail; warn'ы понятны (`.cursor`, чужие skills) |
| M5.2 | `make sdd-test` | нет `warning: overriding recipe` (фикс E2); ruff/pytest либо идут, либо честный skip |
| M5.3 | `make sdd-check` | зелёный; spec-lint виден и назван advisory |
| M5.4 | `make sdd-index` в `~/cybernet/web-backend-new` | инкрементальный апдейт без ключа (уже проверено мной — повторите после `--refresh`) |
| M5.5 | `make sdd-review` | AI-ревью на вашей подписке (я запустить не могу) |

## M6. Store из целевого репо

| # | Команда | Ожидаемое |
|---|---|---|
| M6.1 | `openspec store list` | `cybernet-specs → ~/cybernet/cybernet-specs` |
| M6.2 | `openspec list --specs --store cybernet-specs` | 8 спек |
| M6.3 | `openspec show external-webapi-authorization --type spec --store cybernet-specs` | выводит сверенный контракт |

---

## Порядок

M1 → M4 (хуки и коммиты — самое ценное) → M3 (скилл целиком) → M2 → M5 → M6.
Между блоками я чиню найденное и раскатываю; после каждого моего `--refresh` заново
открывать сессию не нужно, кроме правок `.claude/settings.json` (там нужен рестарт сессии).

## Что я делаю в фоне, пока вы тестируете

- Довожу вторую волну фиксов (P1–P6) и раскатываю.
- Держу журнал `DRYRUN_WEB2318.md` в актуальном виде.
- Перепроверяю каждый ваш результат по файлам, не полагаясь на пересказ.
